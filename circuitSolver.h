#include <iostream>
#include <string>
#include <vector>
#include "token.h"
#include "parser.h"
#include "resistance_cal.h"
#include "capacitance_cal.h"
#include "RC_Circuits.h"
//fourth layer
using namespace std;

    // void add_battery(string name,double value);
    // void add_series(string name, components type,int value);
    // void add_parallel(string name, components type,int value);
    // void add_to_node(string name, components type,int value, int node_num, string relative_comp);
    // void add_to_ground(string name, components type,int value);
    // void ground_node(int node_change);
    // void ground_last();
    // result_file total_resistance();